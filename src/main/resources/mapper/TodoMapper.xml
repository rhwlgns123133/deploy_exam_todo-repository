<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.springtest.mapper.TodoMapper">
    <select id="getTime" resultType="String">
        select now()
    </select>

    <insert id="insert">
        insert into tbl_todo(title, dueDate, writer) values (#{title}, #{dueDate}, #{writer})
    </insert>

    <select id="selectAll" resultType="com.example.springtest.domain.TodoVO">
        select * from tbl_todo order by tno desc
    </select>

    <select id="selectOne" resultType="com.example.springtest.domain.TodoVO">
        select * from tbl_todo WHERE tno = #{tno}
    </select>

    <select id="delet" resultType="com.example.springtest.domain.TodoVO">
        DELETE from tbl_todo WHERE tno = #{tno}
    </select>

    <select id="update" resultType="com.example.springtest.domain.TodoVO">
        update tbl_todo set title = #{title}, dueDate = #{dueDate},  finished = #{finished} WHERE tno = #{tno}
    </select>

    <sql id="search">
        <where>
            <if test="types != null and types.length> 0">
                <foreach collection="types" item="type" open="(" close=")" separator=" OR">
                    <if test="type == 't'.toString()">
                        title like concat('%' #{keyword}, '%')
                    </if>
                    <if test="type == 'w'.toString()">
                        writer like concat('%' #{keyword}, '%')
                    </if>
                </foreach>
            </if>
            <if test="finished">
                <trim prefix="and">
                    finished = 1
                </trim>
            </if>
            <if test="from != null and to != null">
                <trim prefix="and">
                    dueDate between #{from} and #{to}
                </trim>
            </if>
        </where>
    </sql>

    <select id="selectList" resultType="com.example.springtest.domain.TodoVO">
        select * from tbl_todo
       <include refid="search"></include>
        order by tno desc limit #{skip}, #{size}
    </select>

    <select id="getCount" resultType="int">
        select count(tno) from tbl_todo
        <include refid="search"></include>
    </select>


</mapper>